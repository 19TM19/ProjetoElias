const soma = (a,b) => { return a * b }
console.log(soma(5,7))

const pessoa = 'Matheus'
const idade = '22'
const profi = 'Dono de empresa que cria IA (hihi)'

const relatR = {
    pessoa,
    idade,
    profi,
  };
console.log(relatR)